// const TradeHistoryRequest = require('../services/TradeHistory');
// const Assests = require('../services/Assets');

module.exports = {
    createHistory: function (datas) {
        console.log(datas)
        // TradeHistory.createHistory(datas);
    },
    updateHistory: function () {
        return "Update history";
    }
}